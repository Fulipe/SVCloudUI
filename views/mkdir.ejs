<!DOCTYPE html>
<html lang="en">
<%- include('shared/head') %>
<body>
    <%- include('shared/navbar') -%>   
    
    <h2>Criar diretório em:
        <% for( let i = 0; i < dirAtual.length; i++ ) { %>
            <% if (dirAtual[i] == "/") { %>
                /                   
            <% } else {%>
                <%=dirAtual[i]%>/                
            <% } %>
      <% } %>
    </h2>
    <h4><%= msgCheckDir %></h4>
    
    <form action="/navigation/createdir" method="post">
        <div class="row g-3">
            <div class="col-sm-4">
                <input name="nome" type="text" class="form-control" placeholder="Escreve nome do novo diretório">
            </div>
            <div class="col-auto">
                <button type="submit" class="btn btn-success"><b>Criar diretório</b></button>
            </div>
        </div>
    </form>
    
</body>
</html>