
<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid justify-content-start">
      <a class="navbar-brand" href="/">SVCloudUI</a>
        <div class="navbar-nav">
          <!-- <a class="nav-link" href="#">Features</a> --> 
          <div class="navCommands">
            <ul>
                <li class="nav-item"> 
                  <a href="/navigation/createdir" class="btn btn-success" id="mkdirBtn">
                    <i class="bi bi-plus-circle"></i><b> Create Folder</b>
                  </a>
                </li>
                <li class="nav-item"> 
                  <a href="/files/uploadFile" class="btn btn-primary">
                    <i class="bi bi-file-earmark-arrow-up"></i><b> Upload </b>
                  </a>
                </li>
                <% 
                let currentPath = ""; 
                dirAtual.forEach((dir, index) => {
                  currentPath += `/${dir}`;
                %>
                  <li class="nav-item nav-path">
                    <button type="button" class="btn btn-light ps-1 pe-1 pb-1 p-0 ">
                      <a href="/data<%= currentPath %>">
                        <h3 class="mb-0">
                          <% if (dir == '/') { %>
                            <%= dir %>
                          <% } else { %>
                            /<%= dir %>
                          <% } %>
                        </h5>
                      </a>
                    </button> 
                  </li>
                <% }) %>               
            </ul>
          </div>
      </div>
  </div>
</nav>